import { Box } from "@chakra-ui/react";

const BottomBar = () => {
  return (
    <Box
      bgColor="dark.100"
      position="fixed"
      bottom="0"
      height={5}
      width="100%"
    />
  );
};

export default BottomBar;
